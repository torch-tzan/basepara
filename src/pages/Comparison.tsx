import AppLayout from "@/components/layout/AppLayout";

const Comparison = () => {
  return (
    <AppLayout title="層級比較">
      <div className="flex items-center justify-center h-full">
        <p className="text-muted-foreground text-lg">設計中</p>
      </div>
    </AppLayout>
  );
};

export default Comparison;
